"use strict";

let userName = "LL";

if (userName === "Alba") {
  console.log(`Bienvenida, ${userName}.`);
} else {
  console.log(
    "Lo siento pero el usuario que has introducido no está registrado."
  );
}
